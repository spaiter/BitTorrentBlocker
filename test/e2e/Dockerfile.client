# Dockerfile for E2E Test Client
FROM python:3.11-alpine

# Install networking tools
RUN apk add --no-cache \
    netcat-openbsd \
    curl \
    bind-tools \
    tcpdump \
    bash

WORKDIR /app

CMD ["/bin/sh"]
