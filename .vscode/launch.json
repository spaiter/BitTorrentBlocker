{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Hello",
            "type": "go",
            "request": "launch",
            "mode": "exec",
            "program": "${workspaceFolder}/bin/hello",
            "preLaunchTask": "go build"
        }
    ]
}